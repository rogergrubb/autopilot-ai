# Full Send AI — Environment Variables
# Copy to .env.local and fill in values

# === Auth (Required) ===
NEXTAUTH_SECRET=           # openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (optional — for "Sign in with Google")
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# === AI Models (Required — at least one) ===
GOOGLE_GENERATIVE_AI_API_KEY=  # Gemini 2.5 Pro/Flash
ANTHROPIC_API_KEY=             # Claude Haiku (Deep Research, Web Browser)
OPENAI_API_KEY=                # DALL-E 3 image generation

# === Database (Required) ===
DATABASE_URL=                  # Neon PostgreSQL connection string

# === Browser (Optional) ===
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=

# === Code Sandbox (Optional) ===
E2B_API_KEY=

# === Pipedream MCP (Optional — 3000+ app integrations) ===
PIPEDREAM_CLIENT_ID=
PIPEDREAM_CLIENT_SECRET=
PIPEDREAM_PROJECT_ID=
PIPEDREAM_PROJECT_ENVIRONMENT=development

# === Phone Calls (Optional) ===
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# === Email (Optional) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=Full Send AI

# === Social Media (Optional) ===
TWITTER_CONSUMER_KEY=
TWITTER_CONSUMER_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_TOKEN_SECRET=
FACEBOOK_APP_ID=
FACEBOOK_PAGE_ID=
FACEBOOK_PAGE_ACCESS_TOKEN=

# === Video Generation (Optional) ===
ELEVENLABS_API_KEY=
NEXT_PUBLIC_APP_URL=
